name: my_ai_test
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  # fake_library_xyz: ^9.9.9  # This library does not exist, so it's commented out

# If you need a specific library, ensure it exists on pub.dev and use the correct version. For example:
#  another_library: ^1.2.3